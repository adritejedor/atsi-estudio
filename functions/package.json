{
  "name": "atsi-estudio-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test lib/**/*.spec.js",
    "serve": "npm run build && firebase emulators:start --only functions,hosting"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.9.2"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
